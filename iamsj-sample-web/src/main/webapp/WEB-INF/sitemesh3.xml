<?xml version="1.0" encoding="UTF-8"?>
<sitemesh>
    <!-- 指明满足“/*”的页面，将被“/WEB-INF/views/decorators/decorator.html”所装饰 -->
    <mapping path="/*" decorator="/WEB-INF/view/decorators/decorator.jsp" />

    <!-- 指明满足“/exclude.jsp*”的页面，将被排除，不被装饰 
 	<mapping path="/index.*" exclue="true" />
    <mapping path="/home.*" exclue="true" />
    <mapping path="/media/*" exclue="true" />
    -->  
   <mapping path="/auth/login.*" exclue="true" />
   <mapping path="/auth/doRegist.*" exclue="true" />
   <mapping path="/auth/regist.*" exclue="true" />
</sitemesh>